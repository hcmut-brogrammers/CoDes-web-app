import { FC } from 'react';
import Typography from '@mui/material/Typography';

const Page: FC = async () => {
  return (
    <div>
      <Typography variant="h1">Blogs Page</Typography>
    </div>
  );
};

export default Page;
